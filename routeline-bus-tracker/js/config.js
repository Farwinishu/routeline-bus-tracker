/* Point this at wherever the RouteLine backend is running.
   Local dev default matches `npm start` in /backend (see backend/README.md). */
const API_BASE = 'http://localhost:4000/api';
const SOCKET_BASE = 'http://localhost:4000';
